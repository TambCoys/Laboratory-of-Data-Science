WITH
MEMBER
  [Dim Artist Geography].[Region].[Totale Italia]
AS
  Aggregate(
    [Dim Artist Geography].[Region].[Region].Members
  )

SELECT
  { [Measures].[Streams First Month] } ON COLUMNS,

  NON EMPTY
  (
    (
      [Dim Date].[Month].Members
      -
      { [Dim Date].[Month].[All] }
    )
    *
    {
      [Dim Artist Geography].[Region].[Region].Members,
      [Dim Artist Geography].[Region].[Totale Italia]
    }
  ) ON ROWS

FROM [Group4_DSV]
WHERE ( [Dim Artist Geography].[Country].&[Italia] );

